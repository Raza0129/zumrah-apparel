
  # Build as per requirements

  This is a code bundle for Build as per requirements. The original project is available at https://www.figma.com/design/YbsyvoIi228Bk7vTyaHPlP/Build-as-per-requirements.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  